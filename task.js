class Task{
    constructor(important, title, desc, priority, dueDate,contact, participants, color ) { 
        this.important = important;
        this.title = title;
        this.desc = desc;
        this.priority = priority;
        this.dueDate = dueDate;
    }
}

test ();{
    console.log("Test");
}